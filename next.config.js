/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ensure we are not trying to export a static site unless intended
  // output: 'export', // Do not use this for Node runtime with dynamic routes
};

module.exports = nextConfig;
